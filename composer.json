{
	"name": "artisan-build/forge-client",
	"description": "A Saloon-based client for Laravel Forge (2025+ Version)",
	"type": "library",
	"license": "MIT",
	"require": {
        "illuminate/support": "^11.36|^12.0|^13.0",
        "saloonphp/laravel-plugin": "^3.0",
        "saloonphp/saloon": "^3.0"

    },
    "require-dev": {
        "larastan/larastan": "^v3.0.2",
        "orchestra/testbench": "^v9.9.0",
        "pestphp/pest": "^v3.7.1",
        "laravel/pint": "^1.19.0",
        "phpstan/phpstan": "^2.1.0",
        "saloonphp/saloon-sdk-generator": "^0.4"
    },
	"autoload": {
		"psr-4": {
			"ArtisanBuild\\ForgeClient\\": "src/",
			"ArtisanBuild\\ForgeClient\\Tests\\": "tests/"
		}
	},
	"minimum-stability": "stable",
	"extra": {
		"laravel": {
			"providers": [
				"ArtisanBuild\\ForgeClient\\Providers\\ForgeClientServiceProvider"
			],
			"aliases": {
				"Forge": "ArtisanBuild\\ForgeClient\\Facades\\Forge"
			}
		}
	},
	"config": {
		"allow-plugins": {
			"pestphp/pest-plugin": true
		}
	},
    "scripts": {
        "post-autoload-dump": "@composer run prepare",
        "clear": "@php vendor/bin/testbench package:purge-bench --ansi",
        "prepare": "@php vendor/bin/testbench package:discover --ansi",
        "build": [
            "@composer run prepare",
            "@php vendor/bin/testbench workbench:build --ansi"
        ],
        "start": [
            "Composer\\Config::disableProcessTimeout",
            "@composer run build",
            "@php vendor/bin/testbench serve"
        ],
        "test": "vendor/bin/pest",
        "test-coverage": "vendor/bin/pest --coverage",
        "lint": "vendor/bin/pint",
        "stan": "vendor/bin/phpstan analyse --memory-limit=-1",
        "ready": [
            "@composer lint",
            "@composer stan",
            "@composer test"
        ]
    }
}
